# Apply multivariate linear regression to a multimember grid

This function can be used after performing annual aggregation and with a
multigrid object. it applies multivariate linear regression per pixel if
spatial averages are not performed or for spatially aggregated data

## Usage

``` r
ens_trends(c4R)
```

## Value

array, without spatial averages, or dataframe, if spatial averages are
performed
