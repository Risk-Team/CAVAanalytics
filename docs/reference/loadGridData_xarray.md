# Load data using xarray

automatically using xarray and converting into C4R list for remote data
access. Only works for data stored at UC server

## Usage

``` r
loadGridData_xarray(dataset, var, lonLim, latLim, years, aggr.m = "none")
```
