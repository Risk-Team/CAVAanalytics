# Calculation of thresholds

Calculation of number of days with certain condition. It can be used
with aggregateGrid.

## Usage

``` r
thrs(col, lowert, uppert)
```

## Arguments

- col:

  numeric vector

- lowert:

  numeric. lower threshold

- uppert:

  numeric. upper threshold

## Value

numeric of length 1
