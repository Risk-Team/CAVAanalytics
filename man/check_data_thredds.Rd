% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_data_access.R
\name{check_data_thredds}
\alias{check_data_thredds}
\title{Check that CORDEX and observation URLs are reachable on THREDDS}
\usage{
check_data_thredds(path.to.data = "CORDEX-CORE", domain = NULL)
}
\arguments{
\item{path.to.data}{`CORDEX-CORE`, `CORDEX-CORE-BC`, or `NULL` to check only
the standard THREDDS URLs for ERA5 and W5E5. Local directory paths are not
supported here.}

\item{domain}{CORDEX domain (e.g. `AFR-22`). Required when `path.to.data` is set.

ERA5 and W5E5 are always included (same URLs as [load_data()] with
`path.to.obs = "ERA5"` or `"W5E5"`).}
}
\value{
Invisibly, a tibble with columns `path`, `role` (`model`, `ERA5`, or
  `W5E5`), and `exists`. The CLI reports how many paths are reachable.
}
\description{
Uses the same inventory and URLs as [load_data()] when
`path.to.data` is `CORDEX-CORE` or `CORDEX-CORE-BC`. Lists every model URL
for the given `domain` and dataset (all experiments). Performs a light HTTP
check only (does not open datasets). Does not use years.
}
\seealso{
[load_data()], [check_data_hub()]
}
